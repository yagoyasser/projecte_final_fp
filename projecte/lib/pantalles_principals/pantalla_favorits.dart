import 'package:flutter/material.dart';

class PantallaFavorits extends StatelessWidget {
  static const int idPagina = 3;
  
  const PantallaFavorits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}